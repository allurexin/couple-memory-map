{
  "name": "couple-memory-map",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A private couple food memory map app installable as a PWA.",
  "scripts": {
    "start": "node app/server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
